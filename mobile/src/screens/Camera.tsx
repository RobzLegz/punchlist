import React from "react";
import CameraScreenContainer from "../components/camera/CameraScreenContainer";
import ScreenModule from "../modules/ScreenModule";

const Camera = () => {
  return (
    <ScreenModule>
      <CameraScreenContainer />
    </ScreenModule>
  );
};

export default Camera;
